
Proximos:
mover allowedFields a capa de servicio? (hoy esta en el router)
                1)      class SubproyectosService {
                            static getAllowedFields() {return allowedFields;}

                2)      class SubproyectosController {
                            static getAllowedFields(req, res, next) {
                                req.allowedFields = SubproyectosService.getAllowedFields();
                                next();
                            }
                
                3)      function parseDevExtremeQuery(req, res, next) {
                            const allowedFields = req.allowedFields;

                4)      subproyectosRouter:
                        subproyectosRouter.get("/", SubproyectosController.getAllowedFields,
                                               parseDevExtremeQuery,  SubproyectosController.getAll);

grabar fecha de modificacion
usar tocken y grabar usuario


Commits realizados:
Endpoint Subproyectos (con join) + ajuste parseDevExtremeQuery para armado de where cuando hay joins  (15/12)
Bug import {z} (14/12)
Endpoints getById, post, put, delete en proyectos + validacion con zod + ajuste handling errors (12/12)
Primera version que ejecuta la consulta de proyects (6/12)
Inicio (5/12)

